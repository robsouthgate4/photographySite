export const addProject = (project) => {
  console.log('adding project:', project);
  return {
      type: 'add',
      user
  };
}

export const getProjects = (filter) => {
  console.log('getting projects:', filter);
  return {
      type: 'add',
      user
  };
}
